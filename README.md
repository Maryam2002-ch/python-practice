# 🐍 Python Practice

A collection of my daily Python exercises.

## Exercises

| File | Description |
|------|-------------|
| `even_odd.py` | Takes a number from user and tells if it's even or odd. Includes input validation and quit option (`q`). |
| `find_max.py` | Takes a list of numbers from user and returns the maximum value. Includes input validation and quit option (`q`). |

## How to run

```bash
#Run even/odd checker
python even_odd.py

#Run find max program
python find_max.py
